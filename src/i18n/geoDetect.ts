// Country/language auto-detection has moved to CountryContext.
// This module is kept as a no-op for backwards compatibility.
export async function detectAndApplyLanguage() {
  // intentionally empty
}
